#pragma once

namespace anita { namespace writers {

        ///
        /// \brief A class that wraps writing InteractionLists, and maps of InteractionsLists to a ROOT TFile
        ///
        class InteractionWriter {

        public:

        private:

        }
    }
}
